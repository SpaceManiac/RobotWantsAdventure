The /php directory contains the PHP part of Robot Wants Adventure - all the database connection stuff.

It also contains the release SWF, the webpage (uses a library from the site it's hosted on), and the levels directory.